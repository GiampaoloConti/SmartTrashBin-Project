READ ME...

To run the docker from the terminal:
	-Ensure docker is installed on the server
	-First, cd to the folder path
	-Once inside the folder ensure there is the "docker-compose.yml" file.
	-Then, run the command: docker compose up -d

ENJOY!